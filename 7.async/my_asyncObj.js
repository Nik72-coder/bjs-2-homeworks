
const user = {
    name: 'Лев Толстой',
    login: 'leo_tolstoy',
    reputation: 97,
    total: 900,
 }

 let name = 'name';
 let reputation = 'reputation';
 let total = 'total';

console.log(`${user[name]}, репутация ${user[reputation]}, сообщений ${user[total]}` )


const AlarmClock = {
    constructor: alarmCollection = [],
    intervalId: id = 0,
    addClock(time, canCall) {
        // let time;
        // let canCall = true;
    },
    removeClock(time) {
        
    }
}
